# NPM configuration for @alexanderfortin/pi-zai-usage

# Use access public by default (for scoped packages)
access=public

# Save exact versions in package-lock.json
save-exact=true

# Ensure strict peer dependencies
strict-peer-deps=true
